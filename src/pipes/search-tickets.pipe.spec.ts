import { SearchTicketsPipe } from './search-tickets.pipe';

describe('SearchTicketsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTicketsPipe();
    expect(pipe).toBeTruthy();
  });
});
