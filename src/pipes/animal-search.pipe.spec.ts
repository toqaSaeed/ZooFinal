import { AnimalSearchPipe } from './animal-search.pipe';

describe('AnimalSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimalSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
